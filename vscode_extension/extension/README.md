# SSH Tunnel Manager Environment

This local UI extension reads the per-PC host/port map created by SSH Tunnel Manager and injects proxy variables into newly created Remote-SSH terminals. It does not edit remote files or restart the remote VS Code Server.
